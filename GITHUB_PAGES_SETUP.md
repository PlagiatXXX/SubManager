# 🚀 GitHub Pages Deployment Guide

Проект успешно настроен для автоматического развёртывания на GitHub Pages!

## ✅ Что было сделано

1. **Обновлена конфигурация Vite** (`vite.config.ts`)

   - Добавлен `base: '/SubManager/'` для корректной работы на GitHub Pages

2. **Создана GitHub Actions workflow** (`.github/workflows/deploy.yml`)

   - Автоматическая сборка при каждом пуше в main
   - Автоматический деплой на GitHub Pages

3. **Все изменения отправлены на GitHub**

## 📋 Что нужно сделать

### Шаг 1: Включить GitHub Pages

1. Перейдите в **Settings** вашего репозитория
2. В левой панели найдите **Pages**
3. В разделе **Build and deployment**:
   - Source: выберите **GitHub Actions**
4. Сохраните изменения

### Шаг 2: Проверить деплоймент

1. Перейдите в **Actions** в верхней панели репозитория
2. Найдите последний workflow `Deploy to GitHub Pages`
3. Дождитесь завершения (зелёная галочка ✅)

### Шаг 3: Получить ссылку на сайт

После успешного деплоя ваш сайт будет доступен по адресу:

```
https://plagiatxxx.github.io/SubManager/
```

(замените `plagiatxxx` на ваш GitHub username)

## 🔄 Как это работает

- **На каждый пуш в main** автоматически:
  1. Создаётся новая сборка (`npm run build`)
  2. Файлы собираются в папку `dist/`
  3. Содержимое загружается на GitHub Pages
  4. Сайт обновляется в течение 1-2 минут

## 🛠 Локальное тестирование

Перед пушем можете проверить сборку локально:

```bash
npm run build
npm run preview
```

## 📝 Переменные окружения (если понадобятся)

Если добавляются секретные переменные, их можно настроить в:

- **Settings** → **Secrets and variables** → **Actions**

## ✨ Готово!

Теперь каждый пуш в GitHub автоматически развернёт ваш проект на GitHub Pages! 🎊

Если есть ошибки в деплойменте, проверьте вкладку **Actions** → последний workflow для просмотра логов.
